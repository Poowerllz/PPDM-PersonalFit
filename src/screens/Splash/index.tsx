import { Center, Spinner } from "native-base";
import React from "react";

const SplashScreen = () => {
  return (
    <Center flex={1}>
      <Spinner />
    </Center>
  );
};

export default SplashScreen;
